import QwenMetalEngine

// Scaffold entry point. Real subcommands arrive with their phases
// (P0B-1 Metal harness onward); until then the CLI just identifies itself.
print("\(EngineInfo.name) v\(EngineInfo.version) — scaffold; no subcommands yet")
