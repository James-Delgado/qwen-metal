# Claude Code personal settings (per-developer, never committed)
.claude/settings.local.json

# macOS
.DS_Store

# Editor config (auto-generated per-developer; repo dev loop is CLI/Xcode)
.vscode/

# Swift build artifacts
.build/
DerivedData/
# Per-user Xcode state at any depth (the old root-anchored
# *.xcodeproj/xcuserdata/ missed QwenMetalApp's nested project)
**/xcuserdata/
# Per-developer signing identity (see QwenMetalApp/Base.xcconfig)
QwenMetalApp/Local.xcconfig
# Per-user Xcode state created when the package is opened in Xcode
.swiftpm/

# Model weights — never in git (multi-GB; pinned by revision in DECISIONS.md)
models/
*.safetensors
*.gguf

# Python tooling
__pycache__/
*.pyc
.venv/
.pytest_cache/
